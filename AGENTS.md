# AGENTS.md — github-portfolio

This is a minimal GitHub Profile/Portfolio repo owned by **TuanDat110**.  
Its purpose is the `README.md` that appears on the GitHub profile page.

## What is here

- **`README.md`** — the profile/portfolio content. Currently a stub.
- `.git` — single commit on `main`, pushed to `origin/main`.

## No tooling

There is no build system, test runner, linter, formatter, CI, package manager, or framework.  
None are needed — this is a prose-only repo.

## Workflow

- Edit `README.md` directly.
- Commit and push to `main`. No branch, review, or deploy process exists.

## Constraints

- Keep `README.md` concise and presentable — it renders on the owner's GitHub profile.
- Markdown only; no external assets unless committed alongside.
